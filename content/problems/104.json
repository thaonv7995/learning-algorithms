{
  "id": 104,
  "title": "Maximum Depth of Binary Tree",
  "difficulty": "Easy",
  "category": "Tree",
  "timeComplexity": "O(n)",
  "spaceComplexity": "O(h)",
  "description": "Độ sâu (số nút trên đường dài nhất từ gốc đến lá) của cây nhị phân.",
  "examples": [
    { "input": "root = [3,9,20,null,null,15,7]", "output": "3" }
  ],
  "approach": "DFS: 1 + max(depth(left), depth(right)). Hoặc BFS đếm số tầng.",
  "memoryTip": "maxDepth(null)=0; leaf depth=1.",
  "solutions": {
    "python": "class Solution:\n    def maxDepth(self, root: Optional[TreeNode]) -> int:\n        if not root: return 0\n        return 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))",
    "cpp": "class Solution {\npublic:\n    int maxDepth(TreeNode* root) {\n        if (!root) return 0;\n        return 1 + max(maxDepth(root->left), maxDepth(root->right));\n    }\n};",
    "c": "/* Đệ quy max depth — xem C++ */"
  },
  "analysis": {
    "correctness": "Mọi đường từ gốc xuống lá được tính.",
    "edgeCases": ["null → 0", "Chỉ gốc → 1"],
    "pitfalls": ["Nhầm depth với số nút"]
  },
  "tier": "done"
}
